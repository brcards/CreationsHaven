// Define Z index for layers, might put somewhere else
// need to refine. just adding while i have the thought

const Layers = {
    background: {
        zIndex: 0,
    },
    board: {
        zIndex: 1,
    },
    cards: {
        zIndex: 2,
    },
    hands: {
        zIndex: 3,
    },
    action: {
        zIndex: 4
    }
};

export default Layers;

